import 'package:flutter/material.dart';

class ButtonHitung extends StatelessWidget {
  const ButtonHitung({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: const Text('Hitung'),
    );
  }
}
