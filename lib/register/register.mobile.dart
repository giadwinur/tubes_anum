part of '_index.dart';

class RegistMobile extends StatelessWidget {
  const RegistMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register Mobile'),
      ),
      body: Container(),
    );
  }
}
